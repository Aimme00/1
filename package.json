{
  "name": "askdata-agent-cloudflare",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/verify-cloudflare.mjs",
    "test:worker": "node --test tests/cloudflare-worker.test.mjs",
    "dev:cloudflare": "wrangler pages dev",
    "deploy:cloudflare": "wrangler pages deploy"
  },
  "devDependencies": {
    "wrangler": "^4.45.0"
  }
}

